version: "3.9"
services:
  web:
    build: .
    image: codezilla-next:dev
    container_name: codezilla-next
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/app
      
      - /app/node_modules
    command: sh -c "npm install && npm run dev"
